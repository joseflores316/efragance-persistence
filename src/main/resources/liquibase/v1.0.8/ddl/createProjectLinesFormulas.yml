databaseChangeLog:
  - changeSet:
      id: create-table-PROJECT_LINES_FORMULAS-v1.0.8
      author: efragance
      comment: create table PROJECT_LINES_FORMULAS
      tag: 1.0.8
      changes:
        - tagDatabase:
            tag: 1.0.8
        - createTable:
            tableName: PROJECT_LINES_FORMULAS
            columns:
              - column:
                  name: FORMULA_ID
                  type: bigint(20)
                  constraints:
                    nullable: false
                    foreignKeyName: FK_PROJECT_LINES_FORMULAS_FORMULA_ID
                    references: FORMULAS(ID)
              - column:
                  name: PROJECT_LINE_ID
                  type: bigint(20)
                  constraints:
                    nullable: false
                    foreignKeyName: FK_PROJECT_LINES_FORMULAS_PROJECT_LINE_ID
                    references: PROJECT_LINES(ID)