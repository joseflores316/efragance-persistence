databaseChangeLog:
  - changeSet:
      id: create-table-MS_FID_REL-v1.0.5
      author: efragance
      comment: Create table MS_FID_REL
      tag: 1.0.5
      changes:
        - tagDatabase:
            tag: 1.0.5
        -  createTable:
             tableName:  MS_FID_REL
             columns:
               -  column:
                    name:  ID
                    type:  bigint(20)
                    autoIncrement: true
                    constraints:
                      primaryKey: true
                      primaryKeyName: PK_MS_FID_REL
                      nullable: false
               -  column:
                   name: FORMULA_CHEM_MS_FILE_ID
                   type: bigint(20)
                   constraints:
                     nullable: false
                     foreignKeyName: FK_MS_FID_REL_FORMULA_CHEM_MS_FILE_ID
                     references: FORMULA_CHEM_MS_FILE(ID)
               -  column:
                    name: FORMULA_CHEM_FID_FILE_ID
                    type: bigint(20)
                    constraints:
                      nullable: false
                      foreignKeyName: FK_MS_FID_REL_FORMULA_CHEM_FID_FILE_ID
                      references: FORMULA_CHEM_FID_FILE(ID)
